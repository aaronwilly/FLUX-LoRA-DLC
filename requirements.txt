# Core runtime deps
# Note: this repo supports a fully-offline install via wheelhouse; see README.
accelerate
diffusers
peft
huggingface_hub
transformers
gradio
spaces
numpy
protobuf
requests
tqdm
pillow
safetensors
sentencepiece 

# IMPORTANT: PyTorch with CUDA must be installed separately!
# DO NOT install torch/torchvision from this file - they will be CPU-only
# Install PyTorch with CUDA support using:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# torch
# torchvision
